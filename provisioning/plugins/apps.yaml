apiVersion: 1

apps:
  - type: 'esetech-grafana-app'
    org_id: 1
    org_name: 'esetech'
    disabled: false
    jsonData:
      apiUrl: http://default-url.com
    secureJsonData:
      apiKey: secret-key
